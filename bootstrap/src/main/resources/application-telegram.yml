# Local 환경 - Telegram Bot 설정
spring:
  config:
    activate:
      on-profile: local

telegram:
  bot:
    token: test-token-for-local
    username: TestBot
    chat-id: 123456789

---
# Production 환경 - Telegram Bot 설정
spring:
  config:
    activate:
      on-profile: prod

telegram:
  bot:
    token: ${TELEGRAM_BOT_TOKEN}
    username: ${TELEGRAM_BOT_USERNAME}
    chat-id: ${TELEGRAM_CHAT_ID}
